/** Dashboard segment — stats stream from page.tsx Suspense; no extra fetches here. */
export default function DashboardRouteLayout({ children }: { children: React.ReactNode }) {
  return children;
}
