"use client";

/**
 * Placeholder למניעת שבירת build כש־`OrderMetaHeader` נמחק אך imports נשארים.
 * כשתחזירו את ה־UI המלא, החליפו את הגוף בלי לשנות שם קובץ אלא אם מעדכנים imports בכל המארזים.
 */
export function OrderMetaHeader(_props: Record<string, unknown>) {
  return null;
}
